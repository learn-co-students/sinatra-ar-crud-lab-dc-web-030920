<html>
    <body>

      <h1>Articles</h1>

            <% @articles.each do |article| %>
            <% article_link = "/articles/#{article.id}"%>
            <h2><a href="<%= article_link %>"><%= article.title%></a></h2>

            <% end %>

    </body>
</html>